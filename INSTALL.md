# Installing dc-plugins

Add the marketplace once per machine (in Claude Code or Claude Desktop):

```
/plugin marketplace add datacraftdevelopment/dc-plugins
```

Then install the plugins you want:

```
/plugin install fm-dc@dc-plugins
/plugin install pm@dc-plugins
```

To pull the latest shipped changes on a machine that already has it:

```
/plugin marketplace update dc-plugins
```
